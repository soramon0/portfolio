-- uncomment these statements before running sqlc
-- for sqlc to generate enum types

-- CREATE TYPE WEBSITE_CONFIG_VALUE AS ENUM ('allow', 'disallow');
-- CREATE TYPE USER_TYPE AS ENUM ('admin', 'user');